{
  "name": "mock-strava",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local mock of the Strava API for SPOTWAR dev — runs the full dev→test pipeline at $0.",
  "scripts": {
    "mock": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@mapbox/polyline": "^1.2.1",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/mapbox__polyline": "^1.0.5",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
